query GetUser($input: GetUserInput!) {
  getUser(input: $input) {
    userId
    username
    email
    profile
    password
    bonusPoints
    role
    phoneNumber
    createdAt
    updatedAt
  }
}
