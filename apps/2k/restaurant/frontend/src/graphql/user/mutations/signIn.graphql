mutation SignIn($input: LoginInput!) {
  userLogin(input: $input) {
    token
    user {
      userId
      username
      email
      profile
      bonusPoints
      role
      phoneNumber
      createdAt
      updatedAt
    }
  }
}
